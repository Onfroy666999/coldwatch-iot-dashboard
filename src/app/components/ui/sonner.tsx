// Sonner Toaster component - using custom ToastContainer instead
// Kept for potential future use
import * as React from "react";

const Toaster = (props: Record<string, unknown>) => {
  return null;
};

export { Toaster };
