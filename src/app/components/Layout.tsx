// Layout component - not used in current state-based navigation
// Kept for potential future react-router integration
export default function Layout({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
